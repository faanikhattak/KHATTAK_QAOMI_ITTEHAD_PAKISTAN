# Single source of truth for the app version — bump this one line on
# every release. Imported by the login/welcome screen and the Home
# AppBar so both always show the same number.
APP_VERSION = "1.0.0"